# Copy to .env and fill DATABASE_URL with your own Postgres connection string.
# Neon: https://neon.tech (free tier, hosted serverless Postgres).

DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
PORT=3000
CORS_ORIGIN="http://localhost:5173"
NODE_ENV=development
